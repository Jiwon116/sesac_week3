<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/item_user_id"
        android:layout_width="48dp"
        android:layout_height="64dp"
        android:gravity="center"
        android:textSize="16sp"
        tools:text="1"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/item_user_info_ly"/>

    <LinearLayout
        android:id="@+id/item_user_info_ly"
        android:layout_width="320dp"
        android:layout_height="64dp"
        android:orientation="vertical"
        android:gravity="center"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toEndOf="@id/item_user_id"
        app:layout_constraintEnd_toStartOf="@id/item_user_check_btn" >


        <TextView
            android:id="@+id/item_user_name"
            android:layout_width="match_parent"
            android:layout_height="32dp"
            android:gravity="center_vertical"
            android:textSize="18sp"
            android:textStyle="bold"
            tools:text="내이름" />

        <TextView
            android:id="@+id/item_user_phone"
            android:layout_width="match_parent"
            android:layout_height="32dp"
            android:gravity="center_vertical"
            android:textSize="14sp"
            android:textStyle="bold"
            tools:text="010-1111-1111" />


    </LinearLayout>

    <CheckBox
        android:id="@+id/item_user_check_btn"
        android:layout_width="48dp"
        android:layout_height="64dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toEndOf="@id/item_user_info_ly"
        app:layout_constraintEnd_toEndOf="parent"/>


</androidx.constraintlayout.widget.ConstraintLayout>